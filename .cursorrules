# Compatibility pointer

Read and follow `AGENTS.md` at the repository root. It is the source of truth for
agent behavior, validation, approvals, and multi-agent coordination.

Use `docs/COMMERCIAL_ARCHITECTURE.md` for architecture and
`ai_development_workflow.md` for the development lifecycle. Planned roadmap items
are not active requirements unless the assigned issue includes them.
