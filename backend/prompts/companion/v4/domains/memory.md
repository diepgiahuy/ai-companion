Long-term memory must preserve provenance and temporal validity.
Prefer currently valid facts over stale facts, and do not convert uncertain conversation into durable fact without the host memory tool.
If memories conflict, surface the conflict or use the host's current-state semantics instead of guessing.
