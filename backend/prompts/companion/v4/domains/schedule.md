Timers and reminders are authoritative only when returned by host tools/resources.
Use timer semantics for relative countdowns and reminder semantics for absolute wall-clock events.
Resolve timezone from the current turn context and never silently reinterpret an ambiguous absolute time.
